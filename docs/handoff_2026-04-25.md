# 🔄 세션 핸드오프 — 2026-04-25

> **이 문서는 다음 Claude 세션이 즉시 이어받을 수 있게 작성됨.**
> 두 개의 별도 세션을 **병렬** 로 실행하는 것을 권장.
> 모든 세션은 `docs/teamharness_war_protocol.md` (무자비한 프로토콜) 을 절대 위반하지 않는다.

---

## 📍 현재 시스템 스냅샷 (Snapshot #handoff-2026-04-25)

### 운영 상태
- **베타 모드**: 사용자 7명 (10명 확장 예정)
- **master HEAD**: `85ad0bb` (origin/master 와 sync, recovery 태그 `recovery-handoff-2026-04-25`)
- **PR #1 (R12 Phase B YOLO)**: 머지 완료 (c65c6e2 이전)
- **PR #2 (cat-identity Tier 1 등록 화면)**: 머지 완료 (`c65c6e2`) + 후속 fix `85ad0bb`

### 작동 중 (성역 — 절대 회귀 금지)
- 홈 대시보드 / 카메라 방송 / 뷰어 / WebRTC P2P / Supabase Auth / RLS
- `/cats/new` 등록 화면 (이름·품종·생년월일·성별 + 옵션 7 + 사진 + HSV)
- YOLOv8n 12-class behavior (방송폰 측 추론, flag OFF default)

### Push 된 미머지 브랜치
- **`origin/feat/cat-identity-tier1-fix-r1`** = `61457f3`
  - master 위에 9 commit (fix-r1 4 + fix-r2 2 + fix-r3 3)
  - 검증: tsc=0, vitest 127 passed, next build 컴파일 성공
  - 다음 세션 #A 에서 PR 만들고 9 연속 PASS 후 머지

### 복구 명령어 (Rollback)
```bash
# 모든 cat-identity fix 되돌리고 PR #2 머지 직후 상태로
git checkout master
git reset --hard recovery-handoff-2026-04-25
git push origin master --force-with-lease  # 사장님 승인 후만!

# 또는 Vercel Instant Rollback: 85ad0bb 이전 deployment 선택
```

---

## 🅰️ 다음 세션 #A — cat-identity Tier 1 fix-r3 PR 마무리

### 목표
`origin/feat/cat-identity-tier1-fix-r1` (61457f3) 를 9 연속 PASS 후 PR → 머지.
DB 마이그 2건 (atomic) 적용까지 완료.

### 진행 상태 요약

**완료된 사이클 (자동 9라운드+ 무자비한 프로토콜)**:
| 라운드 | 결과 | 결함 처리 |
|---|---|---|
| 1차 (master 위 fix-r1) | 8 PASS / 2 REJECT | 8 결함 → fix-r1 4 commit |
| 2차 (fix-r2) | 7 PASS / 3 REJECT | 8 결함 → fix-r2 2 commit |
| 3차 (fix-r3) | **pending** | Dev 완료 (3 commit), QA STRICT 10 Agent 미실행 |

**fix-r3 commit (61457f3 위 3개):**
- `0e01620` fix-r3-1 (R4 가독성 4건): constants HSV_CROP_RATIO + CAT_MESSAGES + logger 한국어 + isWorkerSupported rename
- `a8c89e6` fix-r3-2 (R5 엣지 3건): RegistrationResult.error.catId 옵셔널 + maybeSingle().error 처리 + 함수형 setter
- `61457f3` fix-r3-3 (R8 운영 1건): sql/20260425b 헤더 atomic deploy 5단계 + ARCHITECTURE §11.6.1

### 다음 세션이 할 일 (순서)

1. **QA STRICT 3차 사이클** — 10 Agent 동시 (general-purpose, isolation worktree)
   - 각 Agent 가 `git fetch && git checkout 61457f3` 후 Bash 실측
   - 무효 키워드 차단 prompt (위 헌법 5 RULE 인용)
   - R1 동작 / R2 설계일치 / R3 단순화 / R4 가독성 / R4.5 사용자흐름 / R5 엣지케이스 / R6 성능 / R7 보안 / R8 운영 / R9 최종품질
2. **9 연속 PASS 달성** — REJECT 발견 시 Arch → Dev → QA 사이클 자동 반복
3. **사장님 승인 받아 PR 생성**:
   ```
   gh pr create --base master --head feat/cat-identity-tier1-fix-r1 \
     --title "fix(cat-identity): Tier 1 등록 화면 9라운드 결함 정리 (fix-r1+r2+r3)"
   ```
4. **PR 머지** (사장님 직접 클릭)
5. **DB 마이그 2건 atomic 적용** (Vercel READY+PROMOTED 확인 후):
   - `sql/20260425b_cats_rls_policies.sql` (cats RLS 4 정책)
   - `sql/20260425c_cats_weight_min.sql` (weight_kg CHECK 0.1 강화)
   - 적용 절차: ARCHITECTURE.md §11.6.1 5단계 따름
6. **Vercel READY + PROMOTED 확인 후** 사장님 실기기 테스트 지시

### 성공 기준
- master 머지 후 `/cats/new` 정상 동작 (이미 PR #2 머지로 작동 중)
- 9 연속 PASS 로그가 `docs/cat_identity_tier1_qa_*.md` 에 기록
- `cats` 테이블 RLS 정책 활성 (Supabase advisors clean)

### 주의사항
- **fix-r3 9 commit 은 master 의 PR #2 머지본 위에 stack 되어 있음.** PR 본문에 "이전 PR #2 의 9라운드 후속 정리" 명시.
- DB 마이그는 PR 머지 **후** 적용 (코드가 새 컬럼/CHECK 를 알고 있는 상태에서만).
- weight_kg 강화 CHECK 가 기존 row 와 충돌하지 않는지 `SELECT count(*) FROM cats WHERE weight_kg IS NOT NULL AND weight_kg < 0.1` 먼저 실측.

---

## 🅱️ 다음 세션 #B — LTE/G7 ICE + COEP 별도 PR (병렬 진행 가능)

### 목표
G7 카메라폰의 LTE 환경에서 ICE 실패 / `crossOriginIsolated` 미적용으로 YOLO wasm backend 가 동작 안 하는 이슈를 해결하는 **별도 PR**.

### 배경 (사장님 콘솔 로그 분석 — 2026-04-25)
- **현상**: G7 카메라폰만 ICE candidate 협상이 30 초 안에 안 끝남 → 카메라 화면이 풀려서 홈으로 튕김
- **추측 원인**:
  - LTE NAT traversal 지연 (Cloudflare TURN 우회 빈도 증가)
  - `crossOriginIsolated = false` → onnxruntime-web wasm SIMD threading 비활성 → CPU 추론 fallback → ICE keepalive 와 경합
- **사장님 발언**: "그래서 LTE 에서 카메라 하나만 됐었구나"

### 다음 세션이 할 일 (순서)

1. **현 상태 진단 (Arch Agent)**:
   - `src/components/broadcast/CameraBroadcastClient.tsx` ICE timeout 값 확인
   - `next.config.ts` 또는 middleware 의 COEP/COOP 헤더 확인
   - Cloudflare TURN 사용량 / 실패율 (Cloudflare 대시보드 / Wrangler)
   - YOLO wasm backend 가 실제로 SIMD threading 모드로 떴는지 (onnxruntime-web 로그)
2. **설계 (Arch Agent)**:
   - ICE timeout 30s → 60s (LTE 마진)
   - COEP `require-corp` + COOP `same-origin` 헤더 추가 (Next.js headers 함수)
   - Coverage: 카메라 페이지만 적용해서 다른 페이지 영향 0
3. **구현 (Dev Agent)** — staging/ 또는 별도 feature branch
4. **QA Agent 9 연속 PASS** (Level 3 — WebRTC/보안)
5. **PR 생성** + 사장님 G7 + S9 동시 실기기 테스트

### 주의사항
- **세션 #A 와 충돌 없음**: 수정 영역이 cat-identity 와 분리. 병렬 OK.
- **Level 3** (WebRTC) 이므로 9 연속 PASS 필수.
- **COEP `require-corp`** 적용 시 외부 이미지 / iframe 깨질 수 있음. `crossorigin="anonymous"` 강제 + Supabase Storage CORS 헤더 점검.
- **롤백 경로**: feature flag (예: `NEXT_PUBLIC_LTE_FIX_ENABLED`) 로 OFF 시 기존 헤더 / timeout 유지.

---

## 🟢 두 세션 시작 시 권장 첫 메시지

### 세션 #A 첫 프롬프트
```
docs/handoff_2026-04-25.md 의 "다음 세션 #A — cat-identity Tier 1 fix-r3 PR 마무리"
섹션을 읽고 그대로 진행해. QA STRICT 3차 사이클부터 시작.
무자비한 프로토콜 (docs/teamharness_war_protocol.md) 위반 금지.
```

### 세션 #B 첫 프롬프트
```
docs/handoff_2026-04-25.md 의 "다음 세션 #B — LTE/G7 ICE + COEP 별도 PR"
섹션을 읽고 진단부터 시작해. 세션 #A 와 병렬로 진행.
무자비한 프로토콜 (docs/teamharness_war_protocol.md) 위반 금지.
```

---

## 📋 인수인계 체크리스트

- [x] `docs/teamharness_war_protocol.md` 작성 (성역)
- [x] `CLAUDE.md` 최상단에 프로토콜 reference 추가 (모든 세션 자동 로드)
- [x] `recovery-handoff-2026-04-25` 태그 생성 + origin push
- [x] `feat/cat-identity-tier1-fix-r1` (61457f3) origin push
- [x] 본 핸드오프 문서 작성
- [ ] 사장님 컴퓨터 종료 / 로그아웃 (사장님 직접)
- [ ] 다음 세션 #A 시작 (사장님 새 창)
- [ ] 다음 세션 #B 시작 (사장님 새 창, 병렬)

---

**작성**: 팀장 Agent (메인 세션) — 2026-04-25
**서명 위치**: master `85ad0bb` + tag `recovery-handoff-2026-04-25`
