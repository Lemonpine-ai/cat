/**
 * 대시보드용 카메라 수신 UI — 실제 구현은 catvisor/CameraLiveViewer 에 있습니다.
 * (요청 경로 `components/Dashboard/CameraViewer` 와 동일 동작)
 */
export { CameraLiveViewer as CameraViewer } from "@/components/catvisor/CameraLiveViewer";
