import { CameraPairClient } from "./CameraPairClient";

/**
 * 카메라 기기 페어링 페이지 — 로그인 불필요.
 * 관리자 폰에서 표시한 4자리 코드를 입력해 기기를 등록합니다.
 */
export default function CameraPairPage() {
  return <CameraPairClient />;
}
