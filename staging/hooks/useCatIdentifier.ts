/**
 * cat-identity Tier 1 PR commit 1 적용 — 본 파일은 src/hooks/useCatIdentifier.ts 로 이관됨.
 * staging/ 보존 정책 (CLAUDE.md "파일 삭제 절대 금지") 에 따라 re-export shim 유지.
 * 신규 import 는 src/ 경로 권장.
 */
export * from "../../src/hooks/useCatIdentifier";
